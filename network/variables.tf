variable user_data_path {
  description = "Path to the user data template file"
  default     = "<the right folder>/templates/userdata.tpl"
}

variable get_public_ip_script_path {
  description = "Path to the script that gets the public IP"
  default     = "<the right folder>templates/get_public_ip.sh"
}